const snowflakeCount = 150, snowflakeSymbols = ["❄", "❅", "❆"]; function random(a, b) { return Math.random() * (b - a) + a } function initializeSnowflake(a, b = !1) { a.style.left = `${random(0, 100)}vw`, a.style.top = b ? `-${random(0, 100)}vh` : `-${random(0, 10)}vh`, a.innerText = snowflakeSymbols[Math.floor(Math.random() * snowflakeSymbols.length)], a.style.fontSize = `${random(.8, 2)}em`, a.style.opacity = random(.5, 1), a.classList.remove("flare", "blurred"), a.style.animationDuration = `${random(10, 30)}s`, a.style.animationDelay = b ? `${random(0, 15)}s` : "0s", a.style.setProperty("--horizontal-drift", `${random(-15, 15)}vw`), a.style.setProperty("--rotation", `${random(-360, 360)}deg`), Math.random() > .8 && a.classList.add("flare"), Math.random() > .5 && a.classList.add("blurred"), b && document.body.appendChild(a) } function createAndRecycleSnowflake() { const a = document.createElement("div"); a.classList.add("snowflake"), initializeSnowflake(a, !0), a.addEventListener("animationend", () => { initializeSnowflake(a, !1) }) } for (let i = 0; i < snowflakeCount; i++)createAndRecycleSnowflake(); function gerenciarTemaNatal() { const a = new Date().getMonth(), b = a === 11; if (!b) { const a = document.querySelectorAll(".tema-natal"); a.forEach(b => b.classList.add("tema-natal-hide")); const c = document.querySelector(".chapeu-papai-noel"); c && c.classList.add("tema-natal-hide"), console.log("Tema de Natal ocultado (fora do período de Dezembro).") } else console.log("Tema de Natal ativo (Dezembro).") } document.addEventListener("DOMContentLoaded", gerenciarTemaNatal);